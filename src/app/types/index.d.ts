export type SmallCardProps = {
  id: string;
  name: string;
  image: string | StaticImageData;
};

export type BiggerCardProps = {
  id: string;
  name: string;
  singer: string;
  image: string | StaticImageData;
};
